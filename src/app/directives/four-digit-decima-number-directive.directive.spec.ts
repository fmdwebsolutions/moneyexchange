import { FourDigitDecimaNumberDirectiveDirective } from './four-digit-decima-number-directive.directive';

describe('FourDigitDecimaNumberDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FourDigitDecimaNumberDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
